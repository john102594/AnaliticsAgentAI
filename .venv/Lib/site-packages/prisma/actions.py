# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class NovedadActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Novedad]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Novedad.prisma().query_raw(
            'SELECT * FROM Novedad WHERE id = ?',
            1103527590,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Novedad
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Novedad.prisma().query_first(
            'SELECT * FROM Novedad WHERE fecha = ?',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.NovedadCreateInput,
        include: Optional[types.NovedadInclude] = None
    ) -> _PrismaModelT:
        """Create a new Novedad record.

        Parameters
        ----------
        data
            Novedad record data
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The created Novedad record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Novedad record from just the required fields
        novedad = Novedad.prisma().create(
            data={
                # data to create a Novedad record
                'fecha': 'ggciceaie',
                'turno': 1147902781,
                'maquina': 'cadfabfehe',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.NovedadCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Novedad records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Novedad record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Novedad.prisma().create_many(
            data=[
                {
                    # data to create a Novedad record
                    'fecha': 'dgiiaaijj',
                    'turno': 1508029952,
                    'maquina': 'eigcfgbif',
                },
                {
                    # data to create a Novedad record
                    'fecha': 'bagcfbhiig',
                    'turno': 267834847,
                    'maquina': 'biabhbdai',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.NovedadWhereUniqueInput,
        include: Optional[types.NovedadInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Novedad record.

        Parameters
        ----------
        where
            Novedad filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The deleted Novedad record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedad = Novedad.prisma().delete(
            where={
                'id': 836760821,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.NovedadWhereUniqueInput,
        include: Optional[types.NovedadInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Novedad record.

        Parameters
        ----------
        where
            Novedad filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The found Novedad record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedad = Novedad.prisma().find_unique(
            where={
                'id': 595337866,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.NovedadWhereUniqueInput,
        include: Optional[types.NovedadInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Novedad record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Novedad filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The found Novedad record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedad = Novedad.prisma().find_unique_or_raise(
            where={
                'id': 790425851,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
        include: Optional[types.NovedadInclude] = None,
        order: Optional[Union[types.NovedadOrderByInput, List[types.NovedadOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Novedad records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Novedad records returned
        skip
            Ignore the first N results
        where
            Novedad filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Novedad model
        order
            Order the returned Novedad records by any field
        distinct
            Filter Novedad records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Novedad]
            The list of all Novedad records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Novedad records
        novedads = Novedad.prisma().find_many(take=10)

        # find the first 5 Novedad records ordered by the turno field
        novedads = Novedad.prisma().find_many(
            take=5,
            order={
                'turno': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
        include: Optional[types.NovedadInclude] = None,
        order: Optional[Union[types.NovedadOrderByInput, List[types.NovedadOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Novedad record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Novedad filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Novedad model
        order
            Order the returned Novedad records by any field
        distinct
            Filter Novedad records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Novedad
            The first Novedad record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Novedad record ordered by the maquina field
        novedad = Novedad.prisma().find_first(
            skip=1,
            order={
                'maquina': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
        include: Optional[types.NovedadInclude] = None,
        order: Optional[Union[types.NovedadOrderByInput, List[types.NovedadOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Novedad record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Novedad filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Novedad model
        order
            Order the returned Novedad records by any field
        distinct
            Filter Novedad records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Novedad
            The first Novedad record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Novedad record ordered by the orden field
        novedad = Novedad.prisma().find_first_or_raise(
            skip=1,
            order={
                'orden': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.NovedadUpdateInput,
        where: types.NovedadWhereUniqueInput,
        include: Optional[types.NovedadInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Novedad record.

        Parameters
        ----------
        data
            Novedad record data specifying what to update
        where
            Novedad filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The updated Novedad record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        novedad = Novedad.prisma().update(
            where={
                'id': 2111915288,
            },
            data={
                # data to update the Novedad record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.NovedadWhereUniqueInput,
        data: types.NovedadUpsertInput,
        include: Optional[types.NovedadInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Novedad filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Novedad model

        Returns
        -------
        prisma.models.Novedad
            The created or updated Novedad record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedad = Novedad.prisma().upsert(
            where={
                'id': 1149758321,
            },
            data={
                'create': {
                    'id': 1149758321,
                    'fecha': 'bagcfbhiig',
                    'turno': 267834847,
                    'maquina': 'biabhbdai',
                },
                'update': {
                    'fecha': 'bagcfbhiig',
                    'turno': 267834847,
                    'maquina': 'biabhbdai',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.NovedadUpdateManyMutationInput,
        where: types.NovedadWhereInput,
    ) -> int:
        """Update multiple Novedad records

        Parameters
        ----------
        data
            Novedad data to update the selected Novedad records to
        where
            Filter to select the Novedad records to update

        Returns
        -------
        int
            The total number of Novedad records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Novedad records
        total = Novedad.prisma().update_many(
            data={
                'refer': 'bgeecijdgg'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Novedad records present in the database

        Parameters
        ----------
        select
            Select the Novedad fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Novedad filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NovedadCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Novedad.prisma().count()

        # results: prisma.types.NovedadCountAggregateOutput
        results = Novedad.prisma().count(
            select={
                '_all': True,
                'cump_horas': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.NovedadCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
    ) -> types.NovedadCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.NovedadCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadWhereInput] = None,
        cursor: Optional[types.NovedadWhereUniqueInput] = None,
    ) -> Union[int, types.NovedadCountAggregateOutput]:
        """Count the number of Novedad records present in the database

        Parameters
        ----------
        select
            Select the Novedad fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Novedad filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NovedadCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Novedad.prisma().count()

        # results: prisma.types.NovedadCountAggregateOutput
        results = Novedad.prisma().count(
            select={
                '_all': True,
                'cump_metros': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.NovedadCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.NovedadWhereInput] = None
    ) -> int:
        """Delete multiple Novedad records.

        Parameters
        ----------
        where
            Optional Novedad filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Novedad records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Novedad records
        total = Novedad.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.NovedadScalarFieldKeys'],
        *,
        where: Optional['types.NovedadWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.NovedadAvgAggregateInput'] = None,
        sum: Optional['types.NovedadSumAggregateInput'] = None,
        min: Optional['types.NovedadMinAggregateInput'] = None,
        max: Optional['types.NovedadMaxAggregateInput'] = None,
        having: Optional['types.NovedadScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.NovedadCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.NovedadScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.NovedadScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.NovedadGroupByOutput']:
        """Group Novedad records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Novedad fields to group records by
        where
            Novedad filter to select records
        take
            Limit the maximum number of Novedad records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.NovedadGroupByOutput]
            A list of dictionaries representing the Novedad record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Novedad records by cump_t1_t4 values
        # and count how many records are in each group
        results = Novedad.prisma().group_by(
            ['cump_t1_t4'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class HistoricoActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Historico]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Historico.prisma().query_raw(
            'SELECT * FROM Historico WHERE id = ?',
            1388290519,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Historico
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Historico.prisma().query_first(
            'SELECT * FROM Historico WHERE fecha = ?',
            'bgehebiafc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.HistoricoCreateInput,
        include: Optional[types.HistoricoInclude] = None
    ) -> _PrismaModelT:
        """Create a new Historico record.

        Parameters
        ----------
        data
            Historico record data
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The created Historico record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Historico record from just the required fields
        historico = Historico.prisma().create(
            data={
                # data to create a Historico record
                'fecha': 'bghffegacj',
                'turno': 1767274722,
                'maquina': 'dcgchcbbf',
                'proceso': 'bdedcabahc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.HistoricoCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Historico records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Historico record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Historico.prisma().create_many(
            data=[
                {
                    # data to create a Historico record
                    'fecha': 'ghfhiafcb',
                    'turno': 744964398,
                    'maquina': 'bjgjgibgbf',
                    'proceso': 'bbbgbhfjge',
                },
                {
                    # data to create a Historico record
                    'fecha': 'igbehcbab',
                    'turno': 1303003706,
                    'maquina': 'bgiggdidbf',
                    'proceso': 'caaaedabfc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.HistoricoWhereUniqueInput,
        include: Optional[types.HistoricoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Historico record.

        Parameters
        ----------
        where
            Historico filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The deleted Historico record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        historico = Historico.prisma().delete(
            where={
                'id': 1868141281,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.HistoricoWhereUniqueInput,
        include: Optional[types.HistoricoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Historico record.

        Parameters
        ----------
        where
            Historico filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The found Historico record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        historico = Historico.prisma().find_unique(
            where={
                'id': 1860847622,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.HistoricoWhereUniqueInput,
        include: Optional[types.HistoricoInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Historico record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Historico filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The found Historico record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        historico = Historico.prisma().find_unique_or_raise(
            where={
                'id': 1448521415,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
        include: Optional[types.HistoricoInclude] = None,
        order: Optional[Union[types.HistoricoOrderByInput, List[types.HistoricoOrderByInput]]] = None,
        distinct: Optional[List[types.HistoricoScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Historico records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Historico records returned
        skip
            Ignore the first N results
        where
            Historico filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Historico model
        order
            Order the returned Historico records by any field
        distinct
            Filter Historico records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Historico]
            The list of all Historico records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Historico records
        historicos = Historico.prisma().find_many(take=10)

        # find the first 5 Historico records ordered by the turno field
        historicos = Historico.prisma().find_many(
            take=5,
            order={
                'turno': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
        include: Optional[types.HistoricoInclude] = None,
        order: Optional[Union[types.HistoricoOrderByInput, List[types.HistoricoOrderByInput]]] = None,
        distinct: Optional[List[types.HistoricoScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Historico record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Historico filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Historico model
        order
            Order the returned Historico records by any field
        distinct
            Filter Historico records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Historico
            The first Historico record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Historico record ordered by the maquina field
        historico = Historico.prisma().find_first(
            skip=1,
            order={
                'maquina': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
        include: Optional[types.HistoricoInclude] = None,
        order: Optional[Union[types.HistoricoOrderByInput, List[types.HistoricoOrderByInput]]] = None,
        distinct: Optional[List[types.HistoricoScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Historico record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Historico filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Historico model
        order
            Order the returned Historico records by any field
        distinct
            Filter Historico records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Historico
            The first Historico record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Historico record ordered by the proceso field
        historico = Historico.prisma().find_first_or_raise(
            skip=1,
            order={
                'proceso': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.HistoricoUpdateInput,
        where: types.HistoricoWhereUniqueInput,
        include: Optional[types.HistoricoInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Historico record.

        Parameters
        ----------
        data
            Historico record data specifying what to update
        where
            Historico filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The updated Historico record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        historico = Historico.prisma().update(
            where={
                'id': 1628650740,
            },
            data={
                # data to update the Historico record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.HistoricoWhereUniqueInput,
        data: types.HistoricoUpsertInput,
        include: Optional[types.HistoricoInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Historico filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Historico model

        Returns
        -------
        prisma.models.Historico
            The created or updated Historico record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        historico = Historico.prisma().upsert(
            where={
                'id': 1249606685,
            },
            data={
                'create': {
                    'id': 1249606685,
                    'fecha': 'igbehcbab',
                    'turno': 1303003706,
                    'maquina': 'bgiggdidbf',
                    'proceso': 'caaaedabfc',
                },
                'update': {
                    'fecha': 'igbehcbab',
                    'turno': 1303003706,
                    'maquina': 'bgiggdidbf',
                    'proceso': 'caaaedabfc',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.HistoricoUpdateManyMutationInput,
        where: types.HistoricoWhereInput,
    ) -> int:
        """Update multiple Historico records

        Parameters
        ----------
        data
            Historico data to update the selected Historico records to
        where
            Filter to select the Historico records to update

        Returns
        -------
        int
            The total number of Historico records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Historico records
        total = Historico.prisma().update_many(
            data={
                'cump_horas': 835903122.76371
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Historico records present in the database

        Parameters
        ----------
        select
            Select the Historico fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Historico filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.HistoricoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Historico.prisma().count()

        # results: prisma.types.HistoricoCountAggregateOutput
        results = Historico.prisma().count(
            select={
                '_all': True,
                'cump_metros': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.HistoricoCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
    ) -> types.HistoricoCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.HistoricoCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.HistoricoWhereInput] = None,
        cursor: Optional[types.HistoricoWhereUniqueInput] = None,
    ) -> Union[int, types.HistoricoCountAggregateOutput]:
        """Count the number of Historico records present in the database

        Parameters
        ----------
        select
            Select the Historico fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Historico filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.HistoricoCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Historico.prisma().count()

        # results: prisma.types.HistoricoCountAggregateOutput
        results = Historico.prisma().count(
            select={
                '_all': True,
                'cump_desperdicio_pct': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.HistoricoCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.HistoricoWhereInput] = None
    ) -> int:
        """Delete multiple Historico records.

        Parameters
        ----------
        where
            Optional Historico filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Historico records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Historico records
        total = Historico.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.HistoricoScalarFieldKeys'],
        *,
        where: Optional['types.HistoricoWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.HistoricoAvgAggregateInput'] = None,
        sum: Optional['types.HistoricoSumAggregateInput'] = None,
        min: Optional['types.HistoricoMinAggregateInput'] = None,
        max: Optional['types.HistoricoMaxAggregateInput'] = None,
        having: Optional['types.HistoricoScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.HistoricoCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.HistoricoScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.HistoricoScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.HistoricoGroupByOutput']:
        """Group Historico records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Historico fields to group records by
        where
            Historico filter to select records
        take
            Limit the maximum number of Historico records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.HistoricoGroupByOutput]
            A list of dictionaries representing the Historico record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Historico records by cump_productividad_pct values
        # and count how many records are in each group
        results = Historico.prisma().group_by(
            ['cump_productividad_pct'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DesperdicioActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Desperdicio]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Desperdicio.prisma().query_raw(
            'SELECT * FROM Desperdicio WHERE id = ?',
            429995104,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Desperdicio
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Desperdicio.prisma().query_first(
            'SELECT * FROM Desperdicio WHERE fecha = ?',
            'bhhfibbigf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.DesperdicioCreateInput,
        include: Optional[types.DesperdicioInclude] = None
    ) -> _PrismaModelT:
        """Create a new Desperdicio record.

        Parameters
        ----------
        data
            Desperdicio record data
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The created Desperdicio record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Desperdicio record from just the required fields
        desperdicio = Desperdicio.prisma().create(
            data={
                # data to create a Desperdicio record
                'fecha': 'ijdbeffgg',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.DesperdicioCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Desperdicio records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Desperdicio record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Desperdicio.prisma().create_many(
            data=[
                {
                    # data to create a Desperdicio record
                    'fecha': 'jjfeafhfj',
                },
                {
                    # data to create a Desperdicio record
                    'fecha': 'cbachdgfce',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.DesperdicioWhereUniqueInput,
        include: Optional[types.DesperdicioInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Desperdicio record.

        Parameters
        ----------
        where
            Desperdicio filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The deleted Desperdicio record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        desperdicio = Desperdicio.prisma().delete(
            where={
                'id': 271520213,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.DesperdicioWhereUniqueInput,
        include: Optional[types.DesperdicioInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Desperdicio record.

        Parameters
        ----------
        where
            Desperdicio filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The found Desperdicio record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        desperdicio = Desperdicio.prisma().find_unique(
            where={
                'id': 456633834,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.DesperdicioWhereUniqueInput,
        include: Optional[types.DesperdicioInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Desperdicio record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Desperdicio filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The found Desperdicio record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        desperdicio = Desperdicio.prisma().find_unique_or_raise(
            where={
                'id': 2058258651,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
        include: Optional[types.DesperdicioInclude] = None,
        order: Optional[Union[types.DesperdicioOrderByInput, List[types.DesperdicioOrderByInput]]] = None,
        distinct: Optional[List[types.DesperdicioScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Desperdicio records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Desperdicio records returned
        skip
            Ignore the first N results
        where
            Desperdicio filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Desperdicio model
        order
            Order the returned Desperdicio records by any field
        distinct
            Filter Desperdicio records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Desperdicio]
            The list of all Desperdicio records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Desperdicio records
        desperdicios = Desperdicio.prisma().find_many(take=10)

        # find the first 5 Desperdicio records ordered by the hora field
        desperdicios = Desperdicio.prisma().find_many(
            take=5,
            order={
                'hora': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
        include: Optional[types.DesperdicioInclude] = None,
        order: Optional[Union[types.DesperdicioOrderByInput, List[types.DesperdicioOrderByInput]]] = None,
        distinct: Optional[List[types.DesperdicioScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Desperdicio record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Desperdicio filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Desperdicio model
        order
            Order the returned Desperdicio records by any field
        distinct
            Filter Desperdicio records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Desperdicio
            The first Desperdicio record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Desperdicio record ordered by the turno field
        desperdicio = Desperdicio.prisma().find_first(
            skip=1,
            order={
                'turno': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
        include: Optional[types.DesperdicioInclude] = None,
        order: Optional[Union[types.DesperdicioOrderByInput, List[types.DesperdicioOrderByInput]]] = None,
        distinct: Optional[List[types.DesperdicioScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Desperdicio record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Desperdicio filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Desperdicio model
        order
            Order the returned Desperdicio records by any field
        distinct
            Filter Desperdicio records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Desperdicio
            The first Desperdicio record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Desperdicio record ordered by the proceso field
        desperdicio = Desperdicio.prisma().find_first_or_raise(
            skip=1,
            order={
                'proceso': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.DesperdicioUpdateInput,
        where: types.DesperdicioWhereUniqueInput,
        include: Optional[types.DesperdicioInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Desperdicio record.

        Parameters
        ----------
        data
            Desperdicio record data specifying what to update
        where
            Desperdicio filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The updated Desperdicio record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        desperdicio = Desperdicio.prisma().update(
            where={
                'id': 1583689592,
            },
            data={
                # data to update the Desperdicio record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.DesperdicioWhereUniqueInput,
        data: types.DesperdicioUpsertInput,
        include: Optional[types.DesperdicioInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Desperdicio filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Desperdicio model

        Returns
        -------
        prisma.models.Desperdicio
            The created or updated Desperdicio record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        desperdicio = Desperdicio.prisma().upsert(
            where={
                'id': 878442065,
            },
            data={
                'create': {
                    'id': 878442065,
                    'fecha': 'cbachdgfce',
                },
                'update': {
                    'fecha': 'cbachdgfce',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.DesperdicioUpdateManyMutationInput,
        where: types.DesperdicioWhereInput,
    ) -> int:
        """Update multiple Desperdicio records

        Parameters
        ----------
        data
            Desperdicio data to update the selected Desperdicio records to
        where
            Filter to select the Desperdicio records to update

        Returns
        -------
        int
            The total number of Desperdicio records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Desperdicio records
        total = Desperdicio.prisma().update_many(
            data={
                'maquina': 'bghfciaafe'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Desperdicio records present in the database

        Parameters
        ----------
        select
            Select the Desperdicio fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Desperdicio filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DesperdicioCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Desperdicio.prisma().count()

        # results: prisma.types.DesperdicioCountAggregateOutput
        results = Desperdicio.prisma().count(
            select={
                '_all': True,
                'bodega': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.DesperdicioCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
    ) -> types.DesperdicioCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.DesperdicioCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DesperdicioWhereInput] = None,
        cursor: Optional[types.DesperdicioWhereUniqueInput] = None,
    ) -> Union[int, types.DesperdicioCountAggregateOutput]:
        """Count the number of Desperdicio records present in the database

        Parameters
        ----------
        select
            Select the Desperdicio fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Desperdicio filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DesperdicioCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Desperdicio.prisma().count()

        # results: prisma.types.DesperdicioCountAggregateOutput
        results = Desperdicio.prisma().count(
            select={
                '_all': True,
                'o_trabajo': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DesperdicioCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.DesperdicioWhereInput] = None
    ) -> int:
        """Delete multiple Desperdicio records.

        Parameters
        ----------
        where
            Optional Desperdicio filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Desperdicio records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Desperdicio records
        total = Desperdicio.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.DesperdicioScalarFieldKeys'],
        *,
        where: Optional['types.DesperdicioWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DesperdicioAvgAggregateInput'] = None,
        sum: Optional['types.DesperdicioSumAggregateInput'] = None,
        min: Optional['types.DesperdicioMinAggregateInput'] = None,
        max: Optional['types.DesperdicioMaxAggregateInput'] = None,
        having: Optional['types.DesperdicioScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DesperdicioCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DesperdicioScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DesperdicioScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DesperdicioGroupByOutput']:
        """Group Desperdicio records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Desperdicio fields to group records by
        where
            Desperdicio filter to select records
        take
            Limit the maximum number of Desperdicio records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DesperdicioGroupByOutput]
            A list of dictionaries representing the Desperdicio record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Desperdicio records by referencia values
        # and count how many records are in each group
        results = Desperdicio.prisma().group_by(
            ['referencia'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class NovedadImpresionOTActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.NovedadImpresionOT]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = NovedadImpresionOT.prisma().query_raw(
            'SELECT * FROM NovedadImpresionOT WHERE id = ?',
            1627576247,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = NovedadImpresionOT.prisma().query_first(
            'SELECT * FROM NovedadImpresionOT WHERE fecha = ?',
            'cafeiaccbc',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.NovedadImpresionOTCreateInput,
        include: Optional[types.NovedadImpresionOTInclude] = None
    ) -> _PrismaModelT:
        """Create a new NovedadImpresionOT record.

        Parameters
        ----------
        data
            NovedadImpresionOT record data
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The created NovedadImpresionOT record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a NovedadImpresionOT record from just the required fields
        novedadimpresionot = NovedadImpresionOT.prisma().create(
            data={
                # data to create a NovedadImpresionOT record
                'fecha': 'gaddfhfh',
                'turno': 684462146,
                'maquina': 'bgcffadich',
                'ot': 'fcbichhci',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.NovedadImpresionOTCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple NovedadImpresionOT records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of NovedadImpresionOT record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = NovedadImpresionOT.prisma().create_many(
            data=[
                {
                    # data to create a NovedadImpresionOT record
                    'fecha': 'bcggadccgf',
                    'turno': 93253262,
                    'maquina': 'cafdaehjid',
                    'ot': 'gifdddbia',
                },
                {
                    # data to create a NovedadImpresionOT record
                    'fecha': 'bchehecef',
                    'turno': 948921754,
                    'maquina': 'bjgejjabff',
                    'ot': 'bcciijbibg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.NovedadImpresionOTWhereUniqueInput,
        include: Optional[types.NovedadImpresionOTInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single NovedadImpresionOT record.

        Parameters
        ----------
        where
            NovedadImpresionOT filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The deleted NovedadImpresionOT record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedadimpresionot = NovedadImpresionOT.prisma().delete(
            where={
                'id': 255202753,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.NovedadImpresionOTWhereUniqueInput,
        include: Optional[types.NovedadImpresionOTInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique NovedadImpresionOT record.

        Parameters
        ----------
        where
            NovedadImpresionOT filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The found NovedadImpresionOT record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedadimpresionot = NovedadImpresionOT.prisma().find_unique(
            where={
                'id': 1223573862,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.NovedadImpresionOTWhereUniqueInput,
        include: Optional[types.NovedadImpresionOTInclude] = None
    ) -> _PrismaModelT:
        """Find a unique NovedadImpresionOT record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            NovedadImpresionOT filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The found NovedadImpresionOT record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedadimpresionot = NovedadImpresionOT.prisma().find_unique_or_raise(
            where={
                'id': 541269159,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
        include: Optional[types.NovedadImpresionOTInclude] = None,
        order: Optional[Union[types.NovedadImpresionOTOrderByInput, List[types.NovedadImpresionOTOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadImpresionOTScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple NovedadImpresionOT records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of NovedadImpresionOT records returned
        skip
            Ignore the first N results
        where
            NovedadImpresionOT filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model
        order
            Order the returned NovedadImpresionOT records by any field
        distinct
            Filter NovedadImpresionOT records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.NovedadImpresionOT]
            The list of all NovedadImpresionOT records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 NovedadImpresionOT records
        novedadimpresionots = NovedadImpresionOT.prisma().find_many(take=10)

        # find the first 5 NovedadImpresionOT records ordered by the turno field
        novedadimpresionots = NovedadImpresionOT.prisma().find_many(
            take=5,
            order={
                'turno': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
        include: Optional[types.NovedadImpresionOTInclude] = None,
        order: Optional[Union[types.NovedadImpresionOTOrderByInput, List[types.NovedadImpresionOTOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadImpresionOTScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single NovedadImpresionOT record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            NovedadImpresionOT filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model
        order
            Order the returned NovedadImpresionOT records by any field
        distinct
            Filter NovedadImpresionOT records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The first NovedadImpresionOT record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second NovedadImpresionOT record ordered by the maquina field
        novedadimpresionot = NovedadImpresionOT.prisma().find_first(
            skip=1,
            order={
                'maquina': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
        include: Optional[types.NovedadImpresionOTInclude] = None,
        order: Optional[Union[types.NovedadImpresionOTOrderByInput, List[types.NovedadImpresionOTOrderByInput]]] = None,
        distinct: Optional[List[types.NovedadImpresionOTScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single NovedadImpresionOT record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            NovedadImpresionOT filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model
        order
            Order the returned NovedadImpresionOT records by any field
        distinct
            Filter NovedadImpresionOT records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The first NovedadImpresionOT record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second NovedadImpresionOT record ordered by the operario field
        novedadimpresionot = NovedadImpresionOT.prisma().find_first_or_raise(
            skip=1,
            order={
                'operario': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.NovedadImpresionOTUpdateInput,
        where: types.NovedadImpresionOTWhereUniqueInput,
        include: Optional[types.NovedadImpresionOTInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single NovedadImpresionOT record.

        Parameters
        ----------
        data
            NovedadImpresionOT record data specifying what to update
        where
            NovedadImpresionOT filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The updated NovedadImpresionOT record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        novedadimpresionot = NovedadImpresionOT.prisma().update(
            where={
                'id': 1064846676,
            },
            data={
                # data to update the NovedadImpresionOT record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.NovedadImpresionOTWhereUniqueInput,
        data: types.NovedadImpresionOTUpsertInput,
        include: Optional[types.NovedadImpresionOTInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            NovedadImpresionOT filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned NovedadImpresionOT model

        Returns
        -------
        prisma.models.NovedadImpresionOT
            The created or updated NovedadImpresionOT record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        novedadimpresionot = NovedadImpresionOT.prisma().upsert(
            where={
                'id': 508382461,
            },
            data={
                'create': {
                    'id': 508382461,
                    'fecha': 'bchehecef',
                    'turno': 948921754,
                    'maquina': 'bjgejjabff',
                    'ot': 'bcciijbibg',
                },
                'update': {
                    'fecha': 'bchehecef',
                    'turno': 948921754,
                    'maquina': 'bjgejjabff',
                    'ot': 'bcciijbibg',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.NovedadImpresionOTUpdateManyMutationInput,
        where: types.NovedadImpresionOTWhereInput,
    ) -> int:
        """Update multiple NovedadImpresionOT records

        Parameters
        ----------
        data
            NovedadImpresionOT data to update the selected NovedadImpresionOT records to
        where
            Filter to select the NovedadImpresionOT records to update

        Returns
        -------
        int
            The total number of NovedadImpresionOT records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all NovedadImpresionOT records
        total = NovedadImpresionOT.prisma().update_many(
            data={
                'reporte_nro': 'bacecgfhbe'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
    ) -> int:
        """Count the number of NovedadImpresionOT records present in the database

        Parameters
        ----------
        select
            Select the NovedadImpresionOT fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            NovedadImpresionOT filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NovedadImpresionOTCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = NovedadImpresionOT.prisma().count()

        # results: prisma.types.NovedadImpresionOTCountAggregateOutput
        results = NovedadImpresionOT.prisma().count(
            select={
                '_all': True,
                'obs_inicial': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.NovedadImpresionOTCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
    ) -> types.NovedadImpresionOTCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.NovedadImpresionOTCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.NovedadImpresionOTWhereInput] = None,
        cursor: Optional[types.NovedadImpresionOTWhereUniqueInput] = None,
    ) -> Union[int, types.NovedadImpresionOTCountAggregateOutput]:
        """Count the number of NovedadImpresionOT records present in the database

        Parameters
        ----------
        select
            Select the NovedadImpresionOT fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            NovedadImpresionOT filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.NovedadImpresionOTCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = NovedadImpresionOT.prisma().count()

        # results: prisma.types.NovedadImpresionOTCountAggregateOutput
        results = NovedadImpresionOT.prisma().count(
            select={
                '_all': True,
                'obs_final': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.NovedadImpresionOTCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.NovedadImpresionOTWhereInput] = None
    ) -> int:
        """Delete multiple NovedadImpresionOT records.

        Parameters
        ----------
        where
            Optional NovedadImpresionOT filter to find the records to be deleted

        Returns
        -------
        int
            The total number of NovedadImpresionOT records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all NovedadImpresionOT records
        total = NovedadImpresionOT.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.NovedadImpresionOTScalarFieldKeys'],
        *,
        where: Optional['types.NovedadImpresionOTWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.NovedadImpresionOTAvgAggregateInput'] = None,
        sum: Optional['types.NovedadImpresionOTSumAggregateInput'] = None,
        min: Optional['types.NovedadImpresionOTMinAggregateInput'] = None,
        max: Optional['types.NovedadImpresionOTMaxAggregateInput'] = None,
        having: Optional['types.NovedadImpresionOTScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.NovedadImpresionOTCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.NovedadImpresionOTScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.NovedadImpresionOTScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.NovedadImpresionOTGroupByOutput']:
        """Group NovedadImpresionOT records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar NovedadImpresionOT fields to group records by
        where
            NovedadImpresionOT filter to select records
        take
            Limit the maximum number of NovedadImpresionOT records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.NovedadImpresionOTGroupByOutput]
            A list of dictionaries representing the NovedadImpresionOT record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group NovedadImpresionOT records by ot values
        # and count how many records are in each group
        results = NovedadImpresionOT.prisma().group_by(
            ['ot'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ConsumoTintasActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ConsumoTintas]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ConsumoTintas.prisma().query_raw(
            'SELECT * FROM ConsumoTintas WHERE id = ?',
            872078403,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ConsumoTintas
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ConsumoTintas.prisma().query_first(
            'SELECT * FROM ConsumoTintas WHERE orden = ?',
            'biheheiajg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ConsumoTintasCreateInput,
        include: Optional[types.ConsumoTintasInclude] = None
    ) -> _PrismaModelT:
        """Create a new ConsumoTintas record.

        Parameters
        ----------
        data
            ConsumoTintas record data
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The created ConsumoTintas record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ConsumoTintas record from just the required fields
        consumotintas = ConsumoTintas.prisma().create(
            data={
                # data to create a ConsumoTintas record
                'orden': 'jbgijghgb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ConsumoTintasCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ConsumoTintas records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ConsumoTintas record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ConsumoTintas.prisma().create_many(
            data=[
                {
                    # data to create a ConsumoTintas record
                    'orden': 'hgjcghfbi',
                },
                {
                    # data to create a ConsumoTintas record
                    'orden': 'icadbcehj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ConsumoTintasWhereUniqueInput,
        include: Optional[types.ConsumoTintasInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ConsumoTintas record.

        Parameters
        ----------
        where
            ConsumoTintas filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The deleted ConsumoTintas record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        consumotintas = ConsumoTintas.prisma().delete(
            where={
                'id': 92728044,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ConsumoTintasWhereUniqueInput,
        include: Optional[types.ConsumoTintasInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ConsumoTintas record.

        Parameters
        ----------
        where
            ConsumoTintas filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The found ConsumoTintas record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        consumotintas = ConsumoTintas.prisma().find_unique(
            where={
                'id': 344858293,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ConsumoTintasWhereUniqueInput,
        include: Optional[types.ConsumoTintasInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ConsumoTintas record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ConsumoTintas filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The found ConsumoTintas record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        consumotintas = ConsumoTintas.prisma().find_unique_or_raise(
            where={
                'id': 1121741130,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
        include: Optional[types.ConsumoTintasInclude] = None,
        order: Optional[Union[types.ConsumoTintasOrderByInput, List[types.ConsumoTintasOrderByInput]]] = None,
        distinct: Optional[List[types.ConsumoTintasScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ConsumoTintas records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ConsumoTintas records returned
        skip
            Ignore the first N results
        where
            ConsumoTintas filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model
        order
            Order the returned ConsumoTintas records by any field
        distinct
            Filter ConsumoTintas records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ConsumoTintas]
            The list of all ConsumoTintas records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ConsumoTintas records
        consumotintas = ConsumoTintas.prisma().find_many(take=10)

        # find the first 5 ConsumoTintas records ordered by the referencia field
        consumotintas = ConsumoTintas.prisma().find_many(
            take=5,
            order={
                'referencia': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
        include: Optional[types.ConsumoTintasInclude] = None,
        order: Optional[Union[types.ConsumoTintasOrderByInput, List[types.ConsumoTintasOrderByInput]]] = None,
        distinct: Optional[List[types.ConsumoTintasScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ConsumoTintas record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ConsumoTintas filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model
        order
            Order the returned ConsumoTintas records by any field
        distinct
            Filter ConsumoTintas records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ConsumoTintas
            The first ConsumoTintas record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ConsumoTintas record ordered by the linea_financiera field
        consumotintas = ConsumoTintas.prisma().find_first(
            skip=1,
            order={
                'linea_financiera': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
        include: Optional[types.ConsumoTintasInclude] = None,
        order: Optional[Union[types.ConsumoTintasOrderByInput, List[types.ConsumoTintasOrderByInput]]] = None,
        distinct: Optional[List[types.ConsumoTintasScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ConsumoTintas record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ConsumoTintas filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model
        order
            Order the returned ConsumoTintas records by any field
        distinct
            Filter ConsumoTintas records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ConsumoTintas
            The first ConsumoTintas record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ConsumoTintas record ordered by the kilos_tinta field
        consumotintas = ConsumoTintas.prisma().find_first_or_raise(
            skip=1,
            order={
                'kilos_tinta': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ConsumoTintasUpdateInput,
        where: types.ConsumoTintasWhereUniqueInput,
        include: Optional[types.ConsumoTintasInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ConsumoTintas record.

        Parameters
        ----------
        data
            ConsumoTintas record data specifying what to update
        where
            ConsumoTintas filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The updated ConsumoTintas record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        consumotintas = ConsumoTintas.prisma().update(
            where={
                'id': 1495896251,
            },
            data={
                # data to update the ConsumoTintas record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ConsumoTintasWhereUniqueInput,
        data: types.ConsumoTintasUpsertInput,
        include: Optional[types.ConsumoTintasInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ConsumoTintas filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ConsumoTintas model

        Returns
        -------
        prisma.models.ConsumoTintas
            The created or updated ConsumoTintas record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        consumotintas = ConsumoTintas.prisma().upsert(
            where={
                'id': 208521688,
            },
            data={
                'create': {
                    'id': 208521688,
                    'orden': 'icadbcehj',
                },
                'update': {
                    'orden': 'icadbcehj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ConsumoTintasUpdateManyMutationInput,
        where: types.ConsumoTintasWhereInput,
    ) -> int:
        """Update multiple ConsumoTintas records

        Parameters
        ----------
        data
            ConsumoTintas data to update the selected ConsumoTintas records to
        where
            Filter to select the ConsumoTintas records to update

        Returns
        -------
        int
            The total number of ConsumoTintas records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ConsumoTintas records
        total = ConsumoTintas.prisma().update_many(
            data={
                'valor_tintas': 860811569.166093
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ConsumoTintas records present in the database

        Parameters
        ----------
        select
            Select the ConsumoTintas fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ConsumoTintas filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ConsumoTintasCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ConsumoTintas.prisma().count()

        # results: prisma.types.ConsumoTintasCountAggregateOutput
        results = ConsumoTintas.prisma().count(
            select={
                '_all': True,
                'real_tintas_pct': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ConsumoTintasCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
    ) -> types.ConsumoTintasCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ConsumoTintasCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ConsumoTintasWhereInput] = None,
        cursor: Optional[types.ConsumoTintasWhereUniqueInput] = None,
    ) -> Union[int, types.ConsumoTintasCountAggregateOutput]:
        """Count the number of ConsumoTintas records present in the database

        Parameters
        ----------
        select
            Select the ConsumoTintas fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ConsumoTintas filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ConsumoTintasCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ConsumoTintas.prisma().count()

        # results: prisma.types.ConsumoTintasCountAggregateOutput
        results = ConsumoTintas.prisma().count(
            select={
                '_all': True,
                'kilos_impresos': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ConsumoTintasCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ConsumoTintasWhereInput] = None
    ) -> int:
        """Delete multiple ConsumoTintas records.

        Parameters
        ----------
        where
            Optional ConsumoTintas filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ConsumoTintas records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ConsumoTintas records
        total = ConsumoTintas.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ConsumoTintasScalarFieldKeys'],
        *,
        where: Optional['types.ConsumoTintasWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ConsumoTintasAvgAggregateInput'] = None,
        sum: Optional['types.ConsumoTintasSumAggregateInput'] = None,
        min: Optional['types.ConsumoTintasMinAggregateInput'] = None,
        max: Optional['types.ConsumoTintasMaxAggregateInput'] = None,
        having: Optional['types.ConsumoTintasScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ConsumoTintasCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ConsumoTintasScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ConsumoTintasScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ConsumoTintasGroupByOutput']:
        """Group ConsumoTintas records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ConsumoTintas fields to group records by
        where
            ConsumoTintas filter to select records
        take
            Limit the maximum number of ConsumoTintas records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ConsumoTintasGroupByOutput]
            A list of dictionaries representing the ConsumoTintas record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ConsumoTintas records by metros_impresos values
        # and count how many records are in each group
        results = ConsumoTintas.prisma().group_by(
            ['metros_impresos'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TPRActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TPR]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = TPR.prisma().query_raw(
            'SELECT * FROM TPR WHERE id = ?',
            525761943,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TPR
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = TPR.prisma().query_first(
            'SELECT * FROM TPR WHERE proceso = ?',
            'hdgcajhjg',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.TPRCreateInput,
        include: Optional[types.TPRInclude] = None
    ) -> _PrismaModelT:
        """Create a new TPR record.

        Parameters
        ----------
        data
            TPR record data
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The created TPR record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TPR record from just the required fields
        tpr = TPR.prisma().create(
            data={
                # data to create a TPR record
                'proceso': 'ejdjahicb',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.TPRCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TPR records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TPR record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = TPR.prisma().create_many(
            data=[
                {
                    # data to create a TPR record
                    'proceso': 'gdjgigfgc',
                },
                {
                    # data to create a TPR record
                    'proceso': 'gfeaahdeh',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.TPRWhereUniqueInput,
        include: Optional[types.TPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TPR record.

        Parameters
        ----------
        where
            TPR filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The deleted TPR record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tpr = TPR.prisma().delete(
            where={
                'id': 1905261552,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.TPRWhereUniqueInput,
        include: Optional[types.TPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TPR record.

        Parameters
        ----------
        where
            TPR filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The found TPR record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tpr = TPR.prisma().find_unique(
            where={
                'id': 78746985,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.TPRWhereUniqueInput,
        include: Optional[types.TPRInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TPR record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TPR filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The found TPR record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tpr = TPR.prisma().find_unique_or_raise(
            where={
                'id': 1398328302,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
        include: Optional[types.TPRInclude] = None,
        order: Optional[Union[types.TPROrderByInput, List[types.TPROrderByInput]]] = None,
        distinct: Optional[List[types.TPRScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TPR records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TPR records returned
        skip
            Ignore the first N results
        where
            TPR filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TPR model
        order
            Order the returned TPR records by any field
        distinct
            Filter TPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TPR]
            The list of all TPR records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TPR records
        tprs = TPR.prisma().find_many(take=10)

        # find the first 5 TPR records ordered by the maquina field
        tprs = TPR.prisma().find_many(
            take=5,
            order={
                'maquina': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
        include: Optional[types.TPRInclude] = None,
        order: Optional[Union[types.TPROrderByInput, List[types.TPROrderByInput]]] = None,
        distinct: Optional[List[types.TPRScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TPR record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TPR filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TPR model
        order
            Order the returned TPR records by any field
        distinct
            Filter TPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TPR
            The first TPR record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TPR record ordered by the fecha_reporte field
        tpr = TPR.prisma().find_first(
            skip=1,
            order={
                'fecha_reporte': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
        include: Optional[types.TPRInclude] = None,
        order: Optional[Union[types.TPROrderByInput, List[types.TPROrderByInput]]] = None,
        distinct: Optional[List[types.TPRScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TPR record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TPR filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TPR model
        order
            Order the returned TPR records by any field
        distinct
            Filter TPR records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TPR
            The first TPR record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TPR record ordered by the horas field
        tpr = TPR.prisma().find_first_or_raise(
            skip=1,
            order={
                'horas': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.TPRUpdateInput,
        where: types.TPRWhereUniqueInput,
        include: Optional[types.TPRInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TPR record.

        Parameters
        ----------
        data
            TPR record data specifying what to update
        where
            TPR filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The updated TPR record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        tpr = TPR.prisma().update(
            where={
                'id': 856000655,
            },
            data={
                # data to update the TPR record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.TPRWhereUniqueInput,
        data: types.TPRUpsertInput,
        include: Optional[types.TPRInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TPR filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TPR model

        Returns
        -------
        prisma.models.TPR
            The created or updated TPR record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        tpr = TPR.prisma().upsert(
            where={
                'id': 1452336924,
            },
            data={
                'create': {
                    'id': 1452336924,
                    'proceso': 'gfeaahdeh',
                },
                'update': {
                    'proceso': 'gfeaahdeh',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.TPRUpdateManyMutationInput,
        where: types.TPRWhereInput,
    ) -> int:
        """Update multiple TPR records

        Parameters
        ----------
        data
            TPR data to update the selected TPR records to
        where
            Filter to select the TPR records to update

        Returns
        -------
        int
            The total number of TPR records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TPR records
        total = TPR.prisma().update_many(
            data={
                'causal': 'bfhdbjjgfd'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TPR records present in the database

        Parameters
        ----------
        select
            Select the TPR fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TPR filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TPRCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TPR.prisma().count()

        # results: prisma.types.TPRCountAggregateOutput
        results = TPR.prisma().count(
            select={
                '_all': True,
                'tiempo': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.TPRCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
    ) -> types.TPRCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.TPRCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TPRWhereInput] = None,
        cursor: Optional[types.TPRWhereUniqueInput] = None,
    ) -> Union[int, types.TPRCountAggregateOutput]:
        """Count the number of TPR records present in the database

        Parameters
        ----------
        select
            Select the TPR fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TPR filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TPRCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = TPR.prisma().count()

        # results: prisma.types.TPRCountAggregateOutput
        results = TPR.prisma().count(
            select={
                '_all': True,
                'responsable': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TPRCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.TPRWhereInput] = None
    ) -> int:
        """Delete multiple TPR records.

        Parameters
        ----------
        where
            Optional TPR filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TPR records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TPR records
        total = TPR.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.TPRScalarFieldKeys'],
        *,
        where: Optional['types.TPRWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TPRAvgAggregateInput'] = None,
        sum: Optional['types.TPRSumAggregateInput'] = None,
        min: Optional['types.TPRMinAggregateInput'] = None,
        max: Optional['types.TPRMaxAggregateInput'] = None,
        having: Optional['types.TPRScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TPRCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TPRScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TPRScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TPRGroupByOutput']:
        """Group TPR records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TPR fields to group records by
        where
            TPR filter to select records
        take
            Limit the maximum number of TPR records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TPRGroupByOutput]
            A list of dictionaries representing the TPR record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TPR records by orden values
        # and count how many records are in each group
        results = TPR.prisma().group_by(
            ['orden'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models